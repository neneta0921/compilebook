git add ファイル名
新規作成したファイルをリポジトリに加える

git commit -A
作業ツリーに行ったすべての変更をまとめてコミットする（エディタが立ち上がるのでコミットメッセージを入力する）

git reset --hard
前回のコミット以降に作業ツリーに行った変更を全て取り消す

git log -p
過去のコミットを見る

git push
GitHubなどアップストリームにリポジトリをプッシュする

echo "# compilebook" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/neneta0921/compilebook.git
git push -u origin main